# 판매 데이터 분석 및 보고서 생성에 필요한 패키지들
# word_report_generator.py에서 사용하는 패키지들

# 데이터 처리
pandas>=2.0.0

# 시각화
matplotlib>=3.6.0
seaborn>=0.12.0
numpy>=1.24.0

# Excel 파일 처리
openpyxl>=3.1.0

# 워드 문서 생성
python-docx>=1.0.0

# 이메일 전송 (표준 라이브러리이므로 설치 불필요)
# smtplib
# email